<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>org.flowerplatform</groupId>
	<artifactId>flower-platform-rapp-manager-arduino-ide</artifactId>
	<version>1.0.0</version>
	<name>flower-platform-rapp-manager-arduino-ide</name>
	
	<properties>
		<arduino.home>D:/Java/arduino-1.6.7</arduino.home>
		<maven.build.timestamp.format>yyyyMMddHHmm</maven.build.timestamp.format>
		<versionDir>${project.basedir}/src/main/java/org/flowerplatform/rapp_manager/arduino_ide</versionDir>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.flowerplatform</groupId>
			<artifactId>flower-platform-rapp-manager</artifactId>
			<version>1.0.0</version>
		</dependency>
		<dependency>
			<groupId>cc.arduino</groupId>
			<artifactId>arduino-core</artifactId>
			<version>1.0</version>
			<scope>system</scope>
			<systemPath>${arduino.home}/lib/arduino-core.jar</systemPath>
		</dependency>
		<dependency>
			<groupId>processing</groupId>
			<artifactId>processing-app</artifactId>
			<version>1.0</version>
			<scope>system</scope>
			<systemPath>${arduino.home}/lib/pde.jar</systemPath>
		</dependency>
	</dependencies>
	
	<build>
		<finalName>${project.artifactId}-${project.version}-${maven.build.timestamp}</finalName>
		<plugins>
		
			<!-- compile -->
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.3</version>
				<configuration>
					<source>1.7</source>
					<target>1.7</target>
				</configuration>
				<executions>
					<execution>
            			<id>post-compile-version</id>
						<phase>process-classes</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<useIncrementalCompilation>false</useIncrementalCompilation>
						</configuration>
					</execution>
				</executions>
			</plugin>
			
			<!-- replace version in Version.java -->
			<plugin>
				<groupId>com.google.code.maven-replacer-plugin</groupId>
				<artifactId>replacer</artifactId>
				<version>1.5.3</version>
				<inherited>false</inherited>
				<executions>
					<execution>
						<id>set-version-before</id>
						<phase>compile</phase>
						<goals>
							<goal>replace</goal>
						</goals>
						<configuration>
							<file>${versionDir}/Version.java</file>
							<regex>true</regex>
							<replacements>
								<replacement>
									<token>VERSION = .*;</token>
									<value>VERSION = "${project.version}";</value>
								</replacement>
								<replacement>
									<token>BUILD_TIMESTAMP = .*;</token>
									<value>BUILD_TIMESTAMP = "${maven.build.timestamp}";</value>
								</replacement>
							</replacements>
						</configuration>
					</execution>
         		</executions>
			</plugin>

			<!-- backup and restore Version.java -->
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>0</id>
						<phase>generate-sources</phase>
						<configuration>
							<tasks>
								<copy file="${versionDir}/Version.java" tofile="${versionDir}/Version.java.bak" overwrite="true" />
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
					<execution>
						<id>1</id>
						<phase>process-classes</phase>
						<configuration>
							<tasks>
								<move file="${versionDir}/Version.java.bak" tofile="${versionDir}/Version.java" overwrite="true" />
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

        	<!-- create jar bundle -->
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<descriptor>assembly.xml</descriptor>
				</configuration>
			</plugin>
			
		</plugins>
	</build>
	
</project>
